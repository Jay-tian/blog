{% extends 'layout.twig' %}
{% set title =  article.title %}
{% set styles = ['/dist/css/article-show.css'] %}
{{ scripts(scriptList, {0: ['/dist/js/article/show/index.js']}) }}

{% block main %}
  
  <div class="article-main main container">
    <div class="col-md-12">
      <h1 class="title mb3">
        {{ article.title|default() }}
      </h1>
      <div class="content markdown-body">
          {{ article.content|default() }}
      </div>

      {% set commentData =  findComments(article.id, 'article') %}
      <div class="comments">
        {% include 'common/comment/comment.twig'  with {
          targetId: 1,
          targetType: 'article',
          comments: commentData.comments,
          users: commentData.users
        } %}
      </div>
    </div>

    {# <div class="col-md-3">

    </div> #}
  </div>
{% endblock %}